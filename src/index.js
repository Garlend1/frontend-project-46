import _ from 'lodash';
import fs from 'fs';
import path from 'path';
import process from 'process';

const genDiff = (filepath1, filepath2) => {
  const object1 = (fs.readFileSync(), path.resolve(), process.cwd(), _.trim());
}

export default genDiff;
